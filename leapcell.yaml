name: kis-canvas
runtime: docker

build:
  dockerfile: Dockerfile

env:
  - SECRET_KEY=your-production-secret-key-change-this
  - DATABASE_URL=sqlite:///./rplace.db

port: 8000

scale:
  min: 1
  max: 3